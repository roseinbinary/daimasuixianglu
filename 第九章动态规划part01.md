## 🧩 LeetCode 刷题日记  
**日期：** 2025/10/25  
**主题：** 动态规划 Part 01｜斐波那契型转移 · 状态定义 · 空间优化  
**题目：**  
- [509. Fibonacci Number（基础 DP · 状态压缩）](https://leetcode.com/problems/fibonacci-number/)  
- [70. Climbing Stairs（斐波那契 DP · 一维状态）](https://leetcode.com/problems/climbing-stairs/)  
- [746. Min Cost Climbing Stairs（最小代价 · 一维 DP）](https://leetcode.com/problems/min-cost-climbing-stairs/)  

---

### 💡 509. Fibonacci Number（基础 DP · 状态压缩）

- **状态定义：**  
  `dp[i]` 表示第 `i` 个斐波那契数。  

- **状态转移方程：**  
  `dp[i] = dp[i - 1] + dp[i - 2]`

- **初始条件：**  
  `dp[0] = 0, dp[1] = 1`

- **空间优化：**  
  观察到 `dp[i]` 只依赖前两个状态，因此只需维护两个变量：
  ```python
  def fib(n):
      if n <= 1: return n
      a, b = 0, 1
      for _ in range(2, n + 1):
          a, b = b, a + b
      return b
  ```

- **时间复杂度：** O(n)  
- **空间复杂度：** O(1)

---

### 💡 70. Climbing Stairs（斐波那契 DP · 一维状态）

- **题意：** 每次可以爬 1 或 2 阶，问到第 n 阶共有几种爬法。  

- **状态定义：**  
  `dp[i]` 表示爬到第 i 层楼梯的方案数。  

- **状态转移：**
  - 从第 i−1 层走一步；
  - 或从第 i−2 层走两步；
  因此：
  ```
  dp[i] = dp[i - 1] + dp[i - 2]
  ```

- **初始条件：**  
  `dp[1] = 1, dp[2] = 2`

- **实现：**
  ```python
  def climbStairs(n):
      if n <= 2: return n
      a, b = 1, 2
      for _ in range(3, n + 1):
          a, b = b, a + b
      return b
  ```

- **扩展思考（Follow-up）：**  
  若可以一次爬 1~m 阶台阶，属于 **完全背包问题**：
  ```
  dp[i] = sum(dp[i - j])  for j in [1, m]
  ```
  ✅ 留作下次练习。  

---

### 💡 746. Min Cost Climbing Stairs（最小代价 · 一维 DP）

- **题意：**  
  每次可以爬 1 或 2 阶，`cost[i]` 表示踏上第 i 阶需要的体力，  
  求到达楼顶的最小花费。  

- **状态定义：**  
  `dp[i]` = 到达第 i 阶的最小花费。  

- **转移方程：**
  ```
  dp[i] = min(dp[i - 1] + cost[i - 1], 
              dp[i - 2] + cost[i - 2])
  ```
  取更省体力的跳法。  

- **初始化：**  
  `dp[0] = 0, dp[1] = 0`（表示可以从第 0 或第 1 阶起跳）

- **实现：**
  ```python
  def minCostClimbingStairs(cost):
      n = len(cost)
      dp = [0] * (n + 1)
      for i in range(2, n + 1):
          dp[i] = min(dp[i - 1] + cost[i - 1],
                      dp[i - 2] + cost[i - 2])
      return dp[n]
  ```

- **优化版（O(1) 空间）：**
  ```python
  a = b = 0
  for i in range(2, len(cost) + 1):
      a, b = b, min(b + cost[i - 1], a + cost[i - 2])
  return b
  ```

- **关键理解：**  
  “到达第 i 阶的最小花费”是状态，而不是“踩上第 i 阶的花费”。  

---

### ⚡ 易错点 / 卡壳点
- 509：初始条件写错（`dp[0]=0, dp[1]=1`）；  
- 70：混淆“爬到第 i 层”的方案数与“从第 i 层出发”的状态；  
- 746：把“花费”与“台阶位置”混为一谈，导致 off-by-one 错误。  

---

### ✅ 改进方案
- 理解 DP 的四步模板：
  1. 明确状态 `dp[i]` 的含义；
  2. 找出状态转移方程；
  3. 确定初始化；
  4. 确定遍历顺序与边界。  
- 练习通过“观察依赖关系”来优化空间。  
- 下一步目标：尝试完全背包版的爬楼梯问题（m 阶以内任意跳）。  

---

### 💬 一句话总结
这三道题展示了动态规划的核心哲学：  
> **用有限的状态描述无限的可能。**  
当你能清楚地定义“状态代表什么”，DP 就不再是死记硬背，而是推理的自然结果。
